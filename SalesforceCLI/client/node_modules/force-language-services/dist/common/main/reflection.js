"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getCommandFlags = function (reflect) {
    let flags = [];
    Object.keys(reflect.flags).forEach(function (key) {
        let flag = reflect.flags[key];
        flag.commandFlagInfo.type = 'string';
        if (flag.values) {
            flag.commandFlagInfo.values = flag.values;
        }
        if (flag.default) {
            flag.commandFlagInfo.default = flag.default;
        }
        flags.push(flag.commandFlagInfo);
    });
    return flags;
};

//# sourceMappingURL=reflection.js.map
