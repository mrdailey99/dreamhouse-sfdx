import { IConfig } from '@oclif/config';
import { Dictionary, Optional } from '@salesforce/ts-types';
export interface AnalyticsJSONCommand {
    command: string;
    version: Optional<string>;
    plugin: string;
    plugin_version: string;
    os: Optional<string>;
    shell: Optional<string>;
    language: string;
    valid: true;
    runtime: number;
    status: number;
}
export interface AnalyticsJSON {
    schema: 1;
    commands: AnalyticsJSONCommand[];
    install?: string;
    cli?: string;
}
export default class AnalyticsCommand {
    private config;
    constructor(config: IConfig);
    readonly analyticsPath: string;
    record(plugin: Optional<Dictionary>, commandId: string, runtime: number, status: number): Promise<void>;
    clear(): Promise<void>;
    readJSON(): Promise<AnalyticsJSON>;
    private initialAnalyticsJSON;
    private writeJSON;
}
